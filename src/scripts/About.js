import React from 'react';

const About = (props) => {
	return (
		<div>
			<h2>About this application</h2>
			<p>
				This was built from <code>react-spa</code>, a starter kit for React single-page apps.
			</p>
		</div>
	)
};

export default About;
